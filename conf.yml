
tasks:
  - task: http
    name: test module
    path: http://centos:9200/infra-all/_search
    query:
      query: 
        query_string:
          query: 박대우
    execute: |
      cnt = len(expr['hits']['hits'])
      a = 1
    match: 
    debug: cnt + a

  - task: http
    name: test module
    path: http://centos:9200/infra-all/_search
    query:
      query: 
        query_string:
          query: AIX
    execute: |
      cnt = len(expr['hits']['hits'])
    match: 
    debug: expr